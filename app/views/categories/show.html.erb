<p id="notice"><%= notice %></p>

<div>
  <b>Category name:</b>
  <%= @category.category_name.titleize %>
</div>
<div>
	<b>Supercategories</b>
	<% @category.supercategories.each do |s|%>
		<div><%= s.name %></div>
	<% end %>
</div>
<div>	<b># Posts</b>
	<%= @category.post_ids.count %>
</div>

<%= link_to 'Edit', edit_category_path(@category) %> |
<%= link_to 'Back', categories_path %>
