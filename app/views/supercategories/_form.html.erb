<%= form_for(@supercategory) do |f| %>
  <% if @supercategory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@supercategory.errors.count, "error") %> prohibited this supercategory from being saved:</h2>

      <ul>
      <% @supercategory.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
